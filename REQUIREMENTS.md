# obsidian-kb 需求文档（v1.0.0 草案）

> 状态：**待用户确认**（重点确认 §9 脚本清单，以及 §2 决策记录是否完整）  
> 版本基线：v1.0.0（全新开发）  
> skill 名称：obsidian-kb（用户已确认）  
> 日期：2026-08-02  
> 用途：作为后续开发提示词（PROMPT.md）的唯一需求依据

---

## 1. 背景与目标

### 1.1 设计背景

- 目标场景：个人使用 Obsidian 管理知识库，希望由 AI agent 完成记录、整理、搜索、日记、任务、剪藏等日常操作；
- 已有条件：Obsidian 本体具备完整的知识组织能力（日记、属性、标签、Bases、Canvas、模板、附件），Obsidian CLI 可调用这些能力；
- 因此本 skill 采用 **Obsidian 原生 + CLI** 路线：尽量复用原生能力，只做少量薄封装，不重复造轮子。

### 1.2 设计方向

- 以 **Obsidian 本体 + Obsidian CLI** 为核心，**Vault 是唯一数据源**；
- 不构建任何自研引擎与同步机制（无 SQLite 索引、无映射表、无冲突副本、无守护进程）；
- 版本从 **v1.0.0** 开始，全新开发；
- 全部开发内容独立完成，不依赖、不修改任何既有代码或目录；本需求文档与提示词（PROMPT.md）即全部开发依据。

### 1.3 目标

1. 一个基于 Obsidian CLI 的通用知识库管理 skill，覆盖记录、整理、搜索、日记、任务、属性、标签、Bases、Canvas、附件、模板、网页剪藏、HTML 镜像导出；
2. 轻量化：只纳入少量封装脚本（§9 清单：kb_config / kb_env / html_export / update_skill 已实现），其余能力交给 CLI 与 agent；
3. 通用可迁移：配置驱动，不写死 vault 路径、目录习惯与个人偏好；当前 Windows 现役，未来兼容 macOS；
4. 安全：写入/修改/删除通道明确，删除严格走 CLI（进系统回收站），修改与删除前征得同意，Git 提交兜底文件安全。

---

## 2. 关键决策记录（用户已逐项确认）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 架构 | Obsidian 原生知识管理 + Obsidian CLI，Vault 为唯一事实源。搜索不单独做，CLI 已有 `search`，直接交给 agent 调用 |
| 2 | 操作通道 | 整理、写入、修改**只走 CLI**；读取优先 CLI；允许直接读写文件，但仅在少数例外情况兜底（§10.2） |
| 3 | 使用对象 | 个人自用；**不写死**习惯和 vault，要求通用、可迁移、可兼容 |
| 4 | 典型场景 | 以 FPGA 问题记录为原始示例（§4.1），其余建议场景（网页剪藏、日记、任务、Bases、项目笔记等）用户确认均为常用场景，全部覆盖 |
| 5 | 功能取舍 | HTML 镜像导出为必需能力：**不存 vault 内**，导出到 vault 外镜像目录（首次使用时确认位置），每次新增/修改/删除后自动同步更新；Git 提交必需，但**不创建仓库**（用户新建知识库时自建）；删除严格走 CLI（默认进系统回收站，实现时再确认）；修改/删除已有内容需征得同意；不强制去重/标题规范：写入前查找相似内容，重复/高相似交由用户审批 |
| 6 | 知识模型 | 文件夹 + 标签/属性 + Bases 视图的混合结构；日记必须使用 Obsidian 原生日记，**按月切分目录** |
| 7 | 覆盖范围 | 全部：日记、任务、属性、标签、Bases（.base）、Canvas（.canvas）、附件、模板、网页剪藏 |
| 8 | 引擎 | 不构建任何引擎，只纳入少量封装脚本（清单见 §9，**需确认**） |
| 9 | 发布 | 按 §12 发布约定执行（更新日志与设计文档 → 兼容性检查 → 测试 → 打包 → 提交），但本阶段**只产出需求文档与提示词，不执行任何开发/发布操作** |
| 10 | 环境与配置位置 | vault 路径与名称由配置文件保存，仅首次生成时向用户确认并自动写入；支持多 vault；**配置文件不写死在系统环境变量/全局用户目录**，默认写入**大知识库文件夹（Obsidian vault 的上级目录）**，不写入 vault 内部，按项目隔离，多个 agent 并行管理不同知识库时互不冲突（见 §8.1）；Obsidian 不常开（首次调用 CLI 会尝试拉起 Obsidian，本地实测有出入，见 §11）；当前仅 Windows，未来可能 macOS，需保证扩展性与兼容性 |

---

## 3. 范围

### 3.1 功能范围（全部覆盖）

- 笔记创建、读取、搜索、整理（移动/重命名）；
- 原生日记（按月分目录，含日记索引视图）；
- 任务（`- [ ]` 语法 + CLI 任务查询 + Bases 视图聚合）；
- 属性（frontmatter properties）与标签（tags，**积极打标签**）；
- **双向链接**（沉淀、剪藏、相关笔记之间建立链接，确保知识索引）；
- Bases 视图（.base 文件，表格/卡片/列表视图与过滤、公式）；
- Canvas 画布（.canvas 文件，JSON Canvas 规范；思维导图 / 知识图谱 / 项目规划 / 问题拆解）；
- **问题管理**：按「未解决 / 已解决」切分；解决后移入已解决文件夹标记完成，沉淀时创建知识笔记并双向链接回原问题；
- **知识模块**（原「知识与经验」）：按知识类型子目录切分（经验 / 原理 / 工具 / 设计 / 规范 / 案例 / 模板，可动态扩展）；
- 附件管理；
- 模板（CLI 原生 `template=` 支持）；
- 网页剪藏（defuddle 提取 → CLI 写入）；
- **日程管理**（`日程/` 目录 + event 属性，Bases 日历/列表视图呈现）；
- **自动化提醒**（待办/日程含时间信号时自动创建定时提醒；**平台无关**，使用当前 agent 平台可用的自动化能力，不写死特定工具）；
- **看板总览**（vault 根目录看板文件，嵌入 Bases 视图聚合问题/任务/日程/知识/项目/剪藏/日记，初始化可选组件）；
- HTML 镜像导出（vault 外目录，目录结构与 vault 对应）；
- Git 提交（不创建仓库）；
- 多 Vault 配置与切换；
- 首次初始化：配置写入大知识库文件夹（vault 外）并生成用户手册；
- 长期演进：更新日志与设计文档随仓库维护（git 管理、不随包分发），每次更新保证兼容性。

### 3.2 非目标（明确不做）

- 不构建自研知识管理引擎：无 SQLite 索引、无同步映射表、无冲突副本机制；
- 不封装搜索工具（CLI `search` 直接可用）；
- 不做强制去重、不强制标题规范（仅相似内容提示 + 用户审批）；
- 不创建 Git 仓库（用户自建）；
- 不在 vault 内保存 HTML 导出产物；
- 不写死任何 vault 路径、目录习惯、模板或个人偏好（全部可配置）；
- **不预设文件类型目录**（剪藏 / 模板 / 附件 / Bases / Canvas 的位置由用户指令决定，不是预设根目录）；
- **不设收件箱**（无法判定分类时询问用户，不静默写入）；
- **不设归档目录**（不需要归档）。

---

## 4. 用户故事与典型场景

### 4.1 场景 A：随手记录问题（用户提供的原始示例）

> @skill:obsidian-kb 帮我记录一个问题：是关于FPGA中异步复位和同步复位问题的，我在跟随B站小梅哥2024年的FPGA教程进行学习，目前遇到了这个问题，但是还没有学到，把它做一个记录以便我未来学到这个点后进行复盘。这个问题的具体信息是：
>
> ```verilog
> always@(posedge clk) begin
>         if(!rst_n)
>             counter <= 0;
>         else if(counter == 25000000)
>             counter <= 0;
>         else
>             counter <= counter + 1'd1;
>     end
>
>     always@(posedge clk or negedge rst_n ) begin
>         if(!rst_n)
>             led <=1'b0;
>         else if(counter == 25000000)
>             led <= !led;
>     end
> ```
>
> 这两段代码中的or negedge rst_n是否都有必要，为什么，为什么每少一个or negedge rst_n，在进行RTL ANALYSIS后的schematic图里就会多出一个RTL MUX？在硬件中异步复位和同步复位有什么区别？以上是我要记录的问题，你不需要做出回答。

**期望行为**（沉淀为验收用例）：

1. 不回答问题，只做记录；
2. 创建一篇结构化笔记，完整记录问题描述与两段 Verilog 代码（代码块不得丢失格式）；
3. 自动写入属性：`type: question`、`status: pending`、`source`（小梅哥 2024 FPGA 教程 / B站）、`created` 等；
4. 问题笔记写入 `问题/未解决/`，文件名带日期前缀（`YYYY-MM-DD 问题描述.md`）；
5. 写入前用 CLI 搜索是否存在相似内容；若高度相似，展示给用户由其决定（合并/跳过/仍写入），不强制阻断；
6. 完成后若 vault 是 Git 仓库，自动提交；
7. 最后把笔记链接反馈给用户，并提示"解决后可先标记完成，之后可随时发起沉淀"（沉淀动作由用户触发，skill 不自动写日记/不自动创建任务）。

### 4.2 场景 B：网页剪藏

- 用户给出 URL → 用 defuddle 提取正文（`--md`）→ 生成结构化笔记（标题、正文、`source_url`、`source_domain`、`clipped_at`、`type: clip`）→ 存入剪藏目录 → 相似检查 → 反馈链接。

### 4.3 场景 C：日记与复盘

- "记录今天的日记" → 通过 `obsidian daily:*` 使用原生日记；
- 日记按月自动分目录（`YYYY-MM/`，见 FR-7）；
- **问题解决**：用户表示某问题已解决/搞懂 → 问题移入 `问题/已解决/`，标注 `status: done` 与 `resolved`（解决日期），TODO 对应条目勾选；
- **知识沉淀**：用户发起"沉淀这个问题" → agent 判定知识类型并交用户审核 → 在 `知识/<类型>/` 创建知识笔记（文件名带日期前缀），正文双向链接回原问题；原问题保留在 `问题/已解决/` 可追溯，不删除。

### 4.4 场景 D：任务管理

- 在日记或各模块笔记中收集任务（`- [ ]`）→ 用 CLI `tasks` 汇总 → 用 Bases 视图做任务看板（状态、截止、逾期）。

### 4.5 场景 E：项目 / 领域笔记

- 例如 FPGA 学习项目：项目主页 + 课程笔记 + 实验记录 + 问题记录，用标签（`#fpga`、`#课程/小梅哥`）与属性（`project`、`status`）关联；
- 建新项目时可选套用默认目录/模板，全部可配置。

### 4.6 场景 F：Bases / Canvas 视图

- 用 `.base` 做问题看板（未解决/已解决聚合）、知识索引（知识/ 按类型分组）、任务看板、日记索引、阅读清单、项目仪表盘等；
- 用 `.canvas` 做思维导图、知识图谱（相关笔记作为 file 节点连线）、项目规划图、问题拆解图（遵循 JSON Canvas 规范）；
- Bases / Canvas 文件的位置由用户指令决定，不预设固定目录。

### 4.7 场景 G：HTML 跨设备查看（必选流程）

- 手机端看 md 不便时，需要把笔记镜像导出为独立 HTML（vault 外目录，结构对应），直接用浏览器打开；
- 该导出是**每次新增、修改、删除笔记后的固定流程**：镜像 HTML 自动增量更新（删除时同步移除），保证与 vault 始终一致。

### 4.8 场景 H：多 Vault

- 个人库与工作库并存，通过配置与 `vault=<name>` 参数切换，互不干扰。

### 4.9 场景 I：新建大知识库文件夹，首次接入（典型环境）

- **vault 在 agent 项目建立之前就已创建**，名称由用户自定、不统一（如 `MyVault`、`学习库`、`Work` 均可），skill 不得假设或要求固定名称；
- 用户新建一个**大知识库文件夹**，内部包含这个 Obsidian vault，典型结构：

  ```
  大知识库文件夹/
  ├── obsidian-kb.config.json   # skill 配置（首次初始化生成）
  ├── 用户手册.md               # 用户手册（skill 内置模板复制）
  ├── HTML-Export/              # HTML 导出目录（vault 外，名称首次确认）
  └── <Obsidian Vault>/         # Obsidian 知识库，名称由用户在创建时自定
  ```

- 第一次让 agent 管理时：向导**读取用户确认的 vault 实际路径与名称**（不预设、不猜测）→ 配置文件写入**大知识库文件夹根目录**（`obsidian-kb.config.json`，不在 vault 内）→ 用户手册（skill 内置模板，见 FR-19）复制到大知识库文件夹根目录 → HTML 导出目录默认位于大知识库文件夹内（名称与位置由用户确认，见 FR-15）；
- 之后 agent 在大知识库文件夹内工作即可自动发现配置；
- vault 内部始终保持干净，只含用户自己的笔记与 Obsidian 自身的文件，不出现 skill 的配置、手册等无关文件；
- 该场景作为首次初始化的标准验收环境（§14）。

---

## 5. 功能需求

### FR-1 配置与多 Vault

- 配置文件保存：vault 名称 → 路径映射、默认 vault、HTML 导出根目录、偏好项（日记格式、问题/项目/知识目录、TODO 文件、Git 提交开关等）；**不含**剪藏/模板/附件/Bases/Canvas 目录键（文件类型位置以用户指令为准，v2.0 起）；
- 配置文件按发现顺序定位：会话中用户显式指定的路径 → 当前工作目录/项目根目录下的 `obsidian-kb.config.json` → 首次初始化向导（**默认写入大知识库文件夹，即 vault 的上级目录**）；**不自动写入系统环境变量或全局用户目录**，避免多 agent/多项目共享配置冲突；**不写入 vault 内部**；
- 仅首次生成时向用户确认并自动写入；之后提供增删 vault、切换默认 vault 的能力；
- 多 vault 时所有命令显式指定 `vault=<name>`；单 vault 时可省略。

### FR-2 首次初始化

- 检测配置文件：不存在 → 进入初始化向导，依次确认：vault 实际路径与名称、配置文件存放位置（**默认大知识库文件夹，即 vault 的上级目录**，可改）、是否还有其他 vault、目录结构（提供默认模板，用户可改）、HTML 导出根目录、vault 是否已是 Git 仓库；
- **不询问**剪藏 / 模板 / 附件 / Bases / Canvas 的存放位置（v2.0 起：这些是文件类型分类，位置不预设，**使用时以用户指令为准**）；
- **询问是否创建看板**（v2.2.0 起，可选组件）：同意则在 vault 根目录生成 `看板.md` 与一组 `.base` 视图（问题 / 任务 / 日程 / 知识 / 项目 / 剪藏 / 日记）；不同意则仅记录偏好，用户可随时要求按需补建；
- **必须向用户提示**：vault 在 agent 项目建立之前就已存在，名称不统一，skill 只读取用户确认的实际路径/名称，不假设固定名称（如 `MyVault`）；
- 初始化同时将内置用户手册模板复制到大知识库文件夹根目录（FR-19）；
- 只做**检测与配置**，不创建 vault、不创建 Git 仓库、不往 vault 写入任何 skill 专属文件；
- 初始化结果写入配置文件并反馈用户。

### FR-3 读写通道

- **写入/修改/移动/删除：只走 Obsidian CLI**；
- **读取：优先 CLI**（`read` / `search` / `tasks` / `tags` / `backlinks` 等）；
- 直接读写文件仅限 §10.2 例外清单；每次使用直接文件操作时，必须在回复中说明原因。

### FR-3.1 Obsidian 操作委托（v2.3.0）

- **SKILL.md 不编写 Obsidian 操作规范**（CLI 命令、Markdown / Bases / Canvas 语法、
  剪藏提取等）——Obsidian 的具体操作由 `@skill:obsidian-suite` 调度指导，
  各子领域用法分别查看 `obsidian-cli` / `obsidian-markdown` / `obsidian-bases` /
  `json-canvas` / `defuddle` 专用 skill（外部维护、可及时更新）；
- obsidian-kb 的 `references/` 仅保留**实测经验与领域约定**（属性集、目录模板、
  生命周期、看板板块设计、CLI 坑与例外清单），文件标注"仅供参考、需自行验证、
  以专用 skill 为准"；不复制专用 skill 的教程内容；
- obsidian-kb 自有机制（kb_config / kb_env / html_export / update_skill 脚本、
  初始化向导、模块路由、操作日志、自动化提醒、Git 提交）仍由本 skill 编写。

### FR-4 创建与记录

- 使用 CLI `create`（支持 `template=`、`silent` 等参数）；
- **先分类再路由**：收到记录请求先判定模块类型（问题 / 项目 / 剪藏 / 日记 / 知识 / **日程** / 普通），再按规则路由到对应目录；无法判定时询问用户归属，**不静默写入收件箱**；
- 创建**新内容**不需要征得同意；
- 创建前执行相似内容检查（FR-17）；
- 创建完成后自动增量更新 HTML 镜像（FR-15）。

### FR-4.1 问题管理（未解决 / 已解决）

- 问题目录默认 `问题/`，内部固定分 `未解决/` 与 `已解决/` 两个子目录（可配置）；
- **文件名格式统一为 `YYYY-MM-DD 文件名.md`**（日期为问题出现日期；"文件名"用简短描述，如 `2026-08-04 FPGA同步异步复位问题.md`，**避免用长句描述做文件名**）；未解决与已解决文件夹内均采用此命名；
- **记录**：写入 `问题/未解决/`，属性 `type: question`、`status: pending`、`created`（出现日期）；
- **解决**（用户表示已解决/搞懂）：移入 `问题/已解决/`（文件名保持 `YYYY-MM-DD 文件名.md` 不变），属性 `status: done`，**frontmatter 必须含两个日期字段：`created`（问题记录日期）与 `resolved`（解决日期）**，TODO 对应条目勾选；**此时不删除、不自动沉淀**；
- **沉淀**（用户另行发起）：agent 判定知识类型（见 FR-4.2）并交用户审核 → 在 `知识/<类型>/` 创建知识笔记，正文双向链接回原问题；原问题保留在 `问题/已解决/` 供追溯。

### FR-4.2 知识模块（原「知识与经验」→「知识」）

- 目录默认 `知识/`，子目录按**知识类型**切分：初始 7 类（经验 / 原理 / 工具 / 设计 / 规范 / 案例 / 模板），**未来可由 agent 动态添加新类型并向用户确认**；
- 知识文件名格式：`YYYY-MM-DD 标题.md`（日期为沉淀日期，便于排序）；
- 知识笔记属性：`type: knowledge`、`knowledge_type`（所属类型）、`created`、`resolved`（沉淀日期）、`source`；
- 知识类型由 agent 根据内容判定，**必须交用户审核**后创建；

### FR-4.3 日程管理（v2.2.0）

- 日程目录默认 `日程/`（配置键 `scheduleDir`，与问题/项目/日志/知识同级的内容模块）；
- 日程文件名格式：`YYYY-MM-DD 标题.md`（日期为日程日期，便于排序）；
- 日程笔记属性：`type: event`、`date`（开始时间，ISO 格式如 `2026-08-05T15:00`，Bases 可识别为日期类型）、`end`（结束时间，可选）、`location`（地点，可选）、`status`（`scheduled` / `done` / `cancelled`，默认 `scheduled`）；
- **录入**：用户口述日程（如"周五下午 3 点项目评审"）→ agent 解析日期时间 → CLI 创建，打 `日程` 标签与领域标签；**不回答日程以外的技术问题**；
- **完成/取消**：更新 `status` 属性（`done` / `cancelled`），同步 `updated`（精确到分钟）；不移动文件；
- **及时更新**：所有日程相关操作（录入/改期/完成/取消）后，状态与属性立即写入（Bases 视图读属性即时反映，无需重建视图），并在操作日志记录；
- **提醒联动**：日程创建时若用户要求提醒，或含明确时间信号，按 FR-8.1 自动创建提醒；
- **呈现**：`日程` 标签 + `date` 属性通过 Bases 日历/列表视图聚合（FR-10.1），agent 不固化模板。

### FR-5 修改与整理

- 修改、移动、重命名已有内容前，先向用户展示变更方案并征得同意；
- 简单变更优先 CLI 原生命令（`property:set`、`append` 等）；结构性变更（移动/重命名/批量改）需明确说明影响（如链接更新）；
- **每次修改文件后，必须同步更新 frontmatter `updated` 属性为当天日期时间（精确到分钟，如 `2026-08-04T12:55`）**（v2.0 硬性要求）；
- 变更生效后自动增量更新 HTML 镜像（FR-15）并追加操作日志（FR-15.1）。

### FR-6 删除（严格走 CLI + 回收站 + 同意）

- 删除**只允许**通过 CLI 的删除命令（实现阶段先用 `obsidian help` 确认命令名与行为）；
- 根据现有资料，CLI 删除默认进入**系统回收站**（忽略 Obsidian 的"删除到 .trash"设置），实现阶段必须做一次安全测试（建临时笔记 → 删除 → 确认回收站可恢复）并写入 references；
- 删除前必须征得用户同意，并告知"删除后进入系统回收站，可恢复"；
- 严禁用 `rm`、`Remove-Item`、文件系统删除等任何绕过 CLI 的方式删 vault 内容；
- 删除后自动同步移除/更新对应的镜像 HTML（FR-15）。

### FR-7 日记（原生 + 按月切分）

- 必须使用 Obsidian **原生日记**（Daily Notes），通过 `obsidian daily:*` 系列命令读写；
- **按月分目录（年月/日）**：日记日期格式固定为 `YYYY-MM/YYYY-MM-DD`，目录结构为**年月/日**，例如 `2026-08/2026-08-02.md`，不是年/月/日三层目录；官方支持日期格式中的路径分隔符；具体格式首次初始化时确认，默认 `YYYY-MM/YYYY-MM-DD`；
- 提供日记索引 Base 视图（按日记目录/属性过滤）；
- 若 CLI 无法直接修改日记设置项，初始化时给用户一次性配置指引（属于实现细节，需在实现阶段验证 CLI 能力）。

### FR-8 任务

- 任务统一使用 Obsidian 任务语法 `- [ ]`；
- 用 CLI `tasks` 查询/汇总；用 Bases 视图聚合（按状态、截止日期、优先级等）；
- **已完成待办折叠归档（v2.1.0）**：勾选完成（`- [x]`）后，把已完成条目自动移动到
  TODO.md 底部「已完成」折叠块（callout `> [!success]- 已完成`，`-` 即默认折叠）；
  **最新完成的排在最上方，完成越久的越靠下**；进行中条目保持原序在上；
  实现走 SKILL.md 内联工作流（CLI 读取→定位刚完成行→重组→`create overwrite` 写回，
  全程 CLI，不新增脚本）。

### FR-8.1 自动化提醒（v2.2.0，平台无关）

- **触发**：添加待办（FR-8）或日程（FR-4.3）后，agent 自动判断是否包含"时间信号"；
- **判断信号**：
  - **强信号**（直接创建）：用户明确要求提醒（"提醒我 / 到点提醒 / 别忘了"）；含具体时刻（"明天下午 3 点"）；含周期词（"每天 / 每周五 / 每月"）；
  - **弱信号**（创建前询问一次）：仅含截止日期（"8 月 10 日前交报告"）；
  - **无信号**（不创建）：纯任务描述，无任何时间信息；
- **创建方式（平台无关，多 agent 通用）**：使用**当前 agent 平台可用的自动化/定时提醒能力**（WorkBuddy 为 `automation_update`，其他平台用其对应工具），**不在 SKILL.md 中写死任何特定工具名**；一次性提醒用 once + 具体时刻，周期提醒用 recurring + 周期规则（如每周五）；
- **提醒内容**：触发时调用本 skill 读取对应 TODO/日程条目与相关笔记，向用户展示进度并询问是否完成；
- **关联与记录**：创建成功后，在对应 TODO 条目旁标注「已设提醒」（如 `⏰` 标记或文字注明），并记入操作日志（FR-15.1）；
- **管理**：用户要求取消/查看提醒时，agent 用平台对应能力查询并删除；取消同样记入操作日志；
- **红线**：自动化能力缺失或不可用时，提示用户并仅记录待办，不阻塞主流程。

### FR-9 属性、标签与双向链接

- 属性使用 frontmatter；建议默认核心属性集（`type`、`status`、`source`、`created`、`updated`、`tags`、`aliases`），用户可自行增删；
- 标签支持 frontmatter `tags` 与行内 `#tag`；建议使用层级标签（如 `#知识/经验`、`#领域/fpga`、`#课程/小梅哥`）；
- **积极打标签**：创建笔记时根据内容主动打上领域 / 类型 / 来源标签，为 Bases 聚合与检索建立索引；
- **建立双向链接**：知识沉淀时链接原问题、剪藏链接相关笔记、相关知识点之间互相链接，确保知识之间的索引（v2.0 硬性要求）。

### FR-10 Bases 视图

- 支持创建/编辑 `.base` 文件：问题视图、知识索引、任务视图、日记索引、阅读清单、项目视图、日程视图等；
- 遵循 Obsidian Bases 语法（filters / formulas / views / properties），创建后校验 YAML 合法性；
- 语法要点沉淀到 references（FUNCTIONS_REFERENCE 摘要），SKILL.md 只包含核心用法；
- `.base` 文件存放位置由用户指令决定（不预设固定目录）；agent 日常按需创建，不固化模板。

### FR-10.1 看板总览（v2.2.0，初始化可选组件；v2.3.1 调整形态）

- **看板文件**：vault 根目录 `看板.md`（配置键 `dashboardFile`，默认 `看板.md`），
  打开即一屏总览；**看板.md 是唯一看板文件**；
- **形态（v2.3.1）**：**看板.md + 按内容命名的模块 .base（数据源）**——每个有
  内容的模块一个 `.base`（`问题.base` / `日程.base` / `任务.base` / `知识.base` /
  `项目.base` / `剪藏.base` / `日记.base`），看板.md 用 `![[<模块>.base#视图名]]`
  嵌入；**不存在"总看板"级聚合 base 文件**；
- **动态板块（只加载已有模块）**：生成时用 CLI 检查各模块是否已有笔记
  （问题 / 任务 / 日程 / 知识 / 项目 / 剪藏 / 日记），**只为有内容的模块创建
  对应 .base**；看板.md 也只写有内容的板块分区；操作后流程检测到某板块
  **首次出现内容** → 创建对应 .base 并更新看板文件；
- **板块过滤**（默认 7 种，只建已有模块，可按用户指令增删）：
  1. **问题.base**：过滤 `file.inFolder(问题Dir)`，视图分「未解决 / 已解决」；
  2. **任务.base**：过滤 `file.hasTag("task")`，视图分「进行中 / 已完成」，公式列剩余天数 / 是否逾期；
  3. **日程.base**：过滤 `file.hasTag("日程")`，table 视图「日程列表」（按 date 排序）+「即将到来」（`date >= today()`）；**Bases 不支持 calendar 视图类型，一律用 table**；
  4. **知识.base**：过滤 `type == "knowledge"`，按 `knowledge_type` 分组；
  5. **项目.base**：过滤 `file.inFolder(项目Dir)`，按项目名分组；
  6. **剪藏.base**：过滤 `type == "clip"`，按剪藏时间倒序；
  7. **日记.base**：过滤日记目录，按日期倒序；
  （过滤要点见 `references/bases.md`「看板板块」；Bases 语法以 obsidian-bases 为准）
- **样式与交互**：交互全部 Bases 原生（视图切换 / 排序 / 过滤 / 分组 / 点击打开笔记），
  不依赖任何插件；美化用 **CSS snippet（v2.3.0）**——样式写入
  `<vault>/.obsidian/snippets/obsidian-kb-dashboard.css` 并启用（编辑 / 阅读模式
  都生效，`.dashboard` 前缀限定作用域）；写 `.obsidian` 属 Obsidian 自身配置，
  须先征得用户同意；用户不要求美化则跟随 Obsidian 主题；
- **及时更新**：Bases 视图读取笔记属性，问题解决 / 任务勾选 / 日程状态 / 沉淀 /
  剪藏等操作完成后，看板对应板块自动实时反映，**无需重建看板**；每次相关操作后
  agent 验证看板对应板块已正确反映（读一次核对）；
- **创建时机**：首次初始化时询问用户（FR-2）；不同意则随时可要求按需补建；
- **与"不固化模板"决策的关系**：看板是用户指令指定的场景（可选组件），不属于
  agent 日常按需创建 Bases 的固化模板。

### FR-11 Canvas

- 支持创建/编辑 `.canvas` 文件（JSON Canvas 规范 1.0），用于思维导图、知识图谱、项目规划、问题拆解等；
- 保证节点/边 ID 唯一、边引用有效、JSON 可解析；
- `.canvas` 文件存放位置由用户指令决定（不预设固定目录）。

### FR-12 附件

- 附件优先通过 CLI 导入；若 CLI 不支持二进制写入，允许直接复制到用户指定的 vault 内位置（属于 §10.2 少数例外，须说明原因）；
- 附件存放位置由用户指令决定（不预设固定目录）。

### FR-13 模板

- 使用 CLI `create ... template="<模板名>"` 的原生模板能力；
- 模板目录由用户指令决定（不预设固定目录）；
- skill 不内置写死的个人模板，只提供目录约定与使用方式。

### FR-14 网页剪藏

- 用 defuddle 提取正文（`--md`），再通过 CLI 创建笔记；
- 笔记写入属性：`type: clip`、`source_url`、`source_domain`、`clipped_at`；
- 剪藏目标位置由用户指令决定（不预设固定目录）。

### FR-15 HTML 镜像导出（必需能力）

- HTML 镜像导出是**每次写入后的固定流程**：新增、修改、删除笔记后自动增量更新对应镜像（按 mtime 判断；删除时同步移除对应 HTML），保证镜像与 vault 始终一致；
- 将 vault 内笔记按**相对路径镜像**导出到 vault 外目录，例如：`<导出根>/<vault名>/<相对路径>.html`；
- **导出根目录默认位于大知识库文件夹内**（vault 外），例如 `<大知识库文件夹>/HTML-Export/`，在首次初始化时向用户确认名称与位置并写入配置；用户也可选择其他任意 vault 外位置；
- 支持全量导出（初始化/重建）与增量导出，并生成索引页 `index.html`；
- 导出不依赖 Obsidian 处于打开状态；转换保真度目标为"跨设备可读"，不追求与 Obsidian 渲染完全一致；
- 导出目录不在 vault 内，不受 vault 自身管理（是否纳入用户自己的备份/同步由用户决定）。

### FR-15.1 操作日志（vault 外 log 文件夹）

- 在**大知识库文件夹根目录**（vault 同级）建立 `log/` 目录（配置键 `logDir`，默认 `log`），**不在 vault 内**；
- 日志文件按月/日切分：`log/YYYY-MM/YYYY-MM-DD.md`；
- **每次对知识库的操作**（新增 / 修改 / 删除 / 移动 / 沉淀 / 剪藏 / 读取 / 搜索等）后，在当日日志中追加一条记录（时间、操作类型、目标路径、简述）；
- 日志写入走直接文件操作（vault 外文件，不受 CLI 限制；与配置/手册同理）；
- 日志不纳入 HTML 镜像导出范围（vault 外）。

### FR-16 Git 提交（不创建仓库）

- vault 已是 Git 仓库时（用户自建），在重要的写入/修改/删除完成后自动 `git add -A` + `git commit`；
- **永不执行 `git init`**；未初始化为仓库时跳过提交并提示用户；
- 提交信息使用简短中文描述，建议格式 `docs: 简述`（可在配置中关闭自动提交）。

### FR-17 相似内容提示（替代强制去重）

- 写入前用 CLI `search` 查找相似关键词；发现重复或高度相似内容时，**展示给用户，由用户决定**（合并、忽略或仍写入）；
- 不做强制去重、不做标题查重、不强制命名规范。

### FR-18 环境自检

- 首次使用或关键操作前检查：`obsidian` CLI 是否存在、Obsidian 是否运行/能否拉起、配置文件与 vault 路径是否有效；
- 检查失败时给出可执行的修复提示（例如：请先打开 Obsidian、路径不存在等），不静默失败。

### FR-19 用户手册生成

- skill 内置用户手册模板（`assets/user-manual.md`），内容包括：使用说明（如何调用 skill、首次使用流程、常用操作示例）、设计理念（Obsidian 原生 + CLI、Vault 唯一事实源、配置驱动、安全红线）、知识库目录结构说明、常见问题（FAQ）；
- 首次初始化时自动复制到大知识库文件夹根目录（默认文件名 `用户手册.md`）；若已存在则**不覆盖**，只提示用户；
- 手册面向最终用户（中文），不依赖 skill 内部结构即可阅读。

### FR-20 长期演进、更新日志与设计文档

- `CHANGELOG.md`（更新日志，从 v1.0.0 起）与 `DESIGN.md`（设计文档：架构决策、模块职责、数据流、兼容性策略）为**开发期项目文档**，随仓库（git）维护，**不随 skill 包分发**（v1.4.0 起）；
- 每次版本发布必须同步更新两份文档；
- 兼容性要求：升级不得破坏已有配置、vault 内容与已生成的用户手册；配置文件带版本号，schema 变更必须提供迁移路径；破坏性变更必须升主版本并在 CHANGELOG/DESIGN 中说明。

---

## 6. 非功能需求

### NFR-1 跨平台与可扩展

- 当前仅在 Windows 使用，但代码与脚本不得依赖 Windows 专属能力（PowerShell 专用语法、`cmd` 等），为 macOS 迁移预留；
- 推荐脚本使用 Python 标准库（跨平台），shell 命令保持跨平台可移植写法。

### NFR-2 配置驱动、零硬编码

- vault 路径/名称、导出目录、目录结构、模板、日记格式等一律来自配置文件；
- skill 代码中不得出现写死的个人习惯与路径。

### NFR-3 轻量化

- 无引擎、无数据库、无守护进程；
- 封装脚本数量控制在少量（§9 清单），每个脚本职责单一、薄封装；
- SKILL.md 正文精简（<500 行），细节进 references，渐进式加载。

### NFR-4 数据安全

- 删除严格走 CLI（系统回收站，可恢复）；
- 修改/删除已有内容前必须征得同意；
- 有 Git 时自动提交，作为文件安全兜底；
- 所有破坏性操作可追溯（会话中说明变更内容）。

### NFR-5 幂等与可恢复

- 配置初始化可重复执行（已存在则更新而非覆盖丢失）；
- 写入操作重复执行不产生异常副作用；
- CLI 不可用时给出明确提示，不产生半成品状态。

### NFR-6 性能

- 单次操作（记录、搜索、任务查询）在数秒内完成；
- 搜索/读取交给 CLI，脚本不做全库扫描型功能。

### NFR-7 可维护性

- 遵循 skill-creator 规范：`name` + `description` frontmatter、references 渐进披露、scripts 可直接执行；
- 变更走 CHANGELOG（v1.0.0 起），发布走 §12 约定。

### NFR-8 长期演进与兼容性

- 遵循语义化版本；配置文件 schema 带 `version` 字段；
- 每次发布前置条件：CHANGELOG 与 DESIGN 同步更新、兼容性检查通过；
- 兼容性检查内容：旧配置可读取或可按迁移说明升级、vault 内容不受影响、已生成的用户手册不被覆盖；
- 不破坏用户已有数据；破坏性变更需升主版本并提供迁移方案。

---

## 7. 知识组织模型（文件夹 + 标签/属性 + Bases 视图 + 双向链接）

混合结构：**文件夹分类（问题/项目/知识/日志）+ 标签/属性检索 + Bases 视图聚合 + 双向链接索引**。

### 7.1 默认目录模板（可配置，不写死）

```
大知识库文件夹/
├── obsidian-kb.config.json   # skill 配置（vault 外，首次初始化生成）
├── 用户手册.md               # 使用说明与设计理念（skill 内置模板复制）
├── log/                      # 知识库操作日志（vault 外，preferences.logDir）
│   └── YYYY-MM/YYYY-MM-DD.md # 按年月/日切分，每次操作追加记录
├── HTML-Export/              # HTML 导出目录（vault 外，名称首次确认）
└── <Obsidian Vault>/
    ├── .obsidian/            # Obsidian 自身配置（用户已有）
    ├── 问题/                 # 问题管理（preferences.questionDir）
    │   ├── 未解决/           # 进行中的问题（YYYY-MM-DD 文件名.md）
    │   └── 已解决/           # 已解决的问题（YYYY-MM-DD 文件名.md，status=done + resolved）
    ├── 项目/                 # 项目（preferences.projectsDir）
    ├── 日志/                 # 原生日记（preferences.dailyFolder）
    │   └── YYYY-MM/YYYY-MM-DD.md
    ├── 知识/                 # 知识沉淀（原「知识与经验」，preferences.knowledgeDir）
    │   ├── 经验/             # 知识类型子目录（YYYY-MM-DD 标题.md）
    │   ├── 原理/
    │   ├── 工具/
    │   ├── 设计/
    │   ├── 规范/
    │   ├── 案例/
    │   └── 模板/
    ├── 日程/                 # 日程管理（v2.2.0，preferences.scheduleDir）
    │   └── YYYY-MM-DD 标题.md  # 每条日程一篇笔记（type: event + date/end/location/status）
    ├── 看板.md               # 看板总览（v2.2.0 可选组件，preferences.dashboardFile，嵌入 Bases 视图）
    └── TODO.md               # 唯一待办文件（preferences.todoFile）
```

> **v2.0 变化**：
> - 剪藏 / 模板 / 附件 / Bases / Canvas 不再预设固定目录——它们是**文件类型分类**，存放位置**由用户指令决定**（用户可能分散存放于多个文件夹），使用时以用户指令为准；
> - 不设收件箱：无法判定分类时询问用户；
> - 不设归档目录（不需要归档）；
> - 知识类型子目录初始 7 类，未来可由 agent 动态添加并向用户确认；
> - 外层结构（配置文件、用户手册、HTML 导出目录在大知识库文件夹，vault 外）为默认约定；vault 内目录结构在首次初始化时展示供用户确认或修改，用户选定的结构写入配置。
> **注意：vault 名称由用户在创建时自定、不统一**（vault 先于 agent 项目存在），skill 只读取实际名称，不得假设固定名称。

### 7.2 建议默认属性集（可配置）

| 属性 | 说明 |
|---|---|
| `type` | 笔记类型：`question` / `knowledge` / `clip` / `project` / `daily` / `note` 等 |
| `status` | 状态：`pending` / `in-progress` / `done` |
| `knowledge_type` | 知识类型（仅知识笔记）：`经验` / `原理` / `工具` / `设计` / `规范` / `案例` / `模板` 等 |
| `source` | 来源（课程/文章/人物等，如"小梅哥 2024 FPGA 教程"） |
| `source_url` / `source_domain` | 网页剪藏来源 |
| `created` / `updated` | 创建 / 更新时间（**updated 每次修改文件必须同步更新，精确到分钟**） |
| `resolved` | 问题解决日期 / 知识沉淀日期 |
| `due` / `finished_date` | 任务截止/完成时间 |
| `date` / `end` | 日程开始 / 结束时间（仅日程笔记，ISO 格式，Bases 日期类型） |
| `location` | 日程地点（仅日程笔记，可选） |
| `tags` | 标签（也可用行内标签） |

### 7.3 标签与双向链接约定（v2.0 硬性要求）

- **积极打标签**：创建笔记时根据内容主动打层级标签，例如 `#知识/经验`、`#领域/fpga`、`#课程/小梅哥`、`#项目/xxx`；
- 状态类信息优先用 `status` 属性（便于 Bases 过滤），状态标签不强制；
- **双向链接**：知识沉淀时知识笔记必须链接原问题（`[[原问题]]`）；剪藏链接相关笔记；相关知识点之间互相链接，确保知识索引；
- 标签与双向链接是 Bases 聚合与检索的基础，属 v2.0 硬性要求（非可选）。

---

## 8. 配置文件设计

### 8.1 位置与发现规则（用户已确认）

- **典型结构**：大知识库文件夹是 Obsidian vault 的上级目录：

  ```
  大知识库文件夹/
  ├── obsidian-kb.config.json   # 配置（默认位置）
  ├── 用户手册.md               # 用户手册（默认位置）
  ├── HTML-Export/              # HTML 导出目录（默认位置，名称首次确认）
  └── <Obsidian Vault>/         # vault 内部不写入 skill 文件
  ```

- **vault 先于 agent 项目存在，名称不统一**：skill 不得假设固定名称，首次初始化时读取用户确认的实际路径/名称；
- **不写死在系统环境变量或全局用户目录**（如 `%APPDATA%`、`~/.config`），避免多个 agent/项目并行管理不同知识库时共享同一份配置导致冲突；
- 发现顺序：
  1. 会话中用户显式指定的路径（对话中告知或 `--config` 参数）；
  2. 当前工作目录/项目根目录下的 `obsidian-kb.config.json`（通常就是大知识库文件夹）；
  3. 都不存在时进入首次初始化向导，由用户确认 vault 路径后，**默认写入大知识库文件夹（vault 的上级目录）**；
- **默认位置是大知识库文件夹（vault 的上级目录），绝不写入 vault 内部**，防止 Obsidian 知识库内出现无关文件；
- 同一大知识库文件夹内可注册多个 vault（多 vault 共存于一份配置）；不同大知识库文件夹各自持有独立配置，互不影响。

### 8.2 结构示例

```json
{
  "version": 1,
  "defaultVault": "main",
  "vaults": {
    "main": {
      "name": "main",
      "path": "D:\\Vaults\\main"
    },
    "work": {
      "name": "work",
      "path": "D:\\Vaults\\work"
    }
  },
  "exportRoot": "D:\\Exports\\kb-html",
  "preferences": {
    "dailyFormat": "YYYY-MM/YYYY-MM-DD",
    "dailyFolder": "日志",
    "structure": "default",
    "questionDir": "问题",
    "projectsDir": "项目",
    "knowledgeDir": "知识",
    "scheduleDir": "日程",
    "dashboardFile": "看板.md",
    "todoFile": "TODO.md",
    "gitCommit": true
  }
}
```

> v2.0 起 `preferences` 不再包含剪藏 / 模板 / 附件 / Bases / Canvas 目录键（文件类型位置以用户指令为准），不再包含 `inboxDir`（收件箱移除）。v2.2.0 新增 `scheduleDir`（日程目录）与 `dashboardFile`（看板文件）；旧配置经迁移自动补齐。

### 8.3 原则

- 配置文件是保存"位置与习惯"的**唯一**地方；skill 代码零硬编码；
- 首次生成必须经用户确认；后续修改（增删 vault、改导出目录）也要让用户知情；
- 配置位置按项目隔离：默认当前工作目录，禁止默认写入系统环境变量/全局用户目录。

---

## 9. 脚本清单（已实现）

### 9.1 建议纳入（4 个，Python 标准库薄封装、跨平台）

| 脚本 | 职责 | 纳入理由 |
|---|---|---|
| `scripts/kb_config.py` | 配置管理：首次初始化向导、配置发现与读写（默认写入大知识库文件夹/vault 上级目录）、多 vault 管理（注册/列出/移除/默认切换/按名解析/路径校验） | 多 vault 与"不写死"的基石；配置 schema 需要确定性，不能靠 agent 自由发挥 |
| `scripts/html_export.py` | HTML 镜像导出：按相对路径镜像到 vault 外目录、增量导出、生成索引页 | HTML 导出是高频需求，镜像映射与索引逻辑需要确定性实现 |
| `scripts/kb_env.py` | 环境自检：CLI 是否存在、Obsidian 是否运行/可拉起、配置与 vault 路径是否有效 | 首次使用与故障排查的入口，避免 agent 反复试错 |
| `scripts/update_skill.py` | 发布工具：按 §12 发布约定打包 + 提交 | 工具化可减少人工步骤；仅开发期使用 |

### 9.2 建议不纳入

| 不纳入的能力 | 替代方案 |
|---|---|
| 自研引擎工具集（读写、索引、同步、转换等） | 由 Obsidian CLI 原生命令替代 |
| 搜索封装 | CLI `search` 已提供，agent 直接调用 |
| 去重/标题校验 | 需求取消，改为提示词级相似内容检查（FR-17） |
| 回收站相关封装 | CLI 删除默认进系统回收站，无需封装 |
| Git 初始化 | 用户自建仓库 |
| 日记轮转脚本 | 由 Obsidian 日期格式 `YYYY-MM/` 原生实现 |
| 同步映射 / 冲突副本 | 架构取消，Vault 是唯一事实源 |

### 9.3 可选（默认不纳入，如需要再加）

| 脚本 | 说明 |
|---|---|
| `kb_consent_delete.py` | 删除前的确认包装。默认不纳入，由 agent 遵守红线（FR-6）即可；若实现后误删风险仍高，再加 |
| `kb_daily_index.py` | 日记索引生成。默认不纳入，优先用 `.base` 视图实现 |

> 9.1 的 4 个脚本已全部实现（v1.x 起）；9.3 可选脚本未纳入。

### 9.4 内置资产（建议纳入）

| 资产 | 说明 |
|---|---|
| `assets/user-manual.md` | 用户手册模板（使用说明、设计理念、目录结构、FAQ）；首次初始化时复制到大知识库文件夹根目录，已存在则不覆盖 |

> `CHANGELOG.md` 与 `DESIGN.md` 属于开发期项目文档（FR-20），随 git 维护、不随包分发，不属于 assets。

---

## 10. 与 Obsidian CLI 的边界

### 10.1 命令映射（参考，以 `obsidian help` 为准）

| 需求 | CLI 命令（示例） |
|---|---|
| 创建笔记 | `obsidian create name="..." content="..." template="..." silent` |
| 追加内容 | `obsidian append file="..." content="..."` |
| 读取 | `obsidian read file="..."` |
| 搜索 | `obsidian search query="..." limit=10` |
| 任务 | `obsidian tasks daily todo` 等 |
| 标签 | `obsidian tags sort=count counts` |
| 属性 | `obsidian property:set name="..." value="..." file="..."` |
| 原生日记 | `obsidian daily:read` / `obsidian daily:append content="..."` |
| 反链 | `obsidian backlinks file="..."` |
| 打开笔记 | `obsidian open file="..."` |
| 删除 | `obsidian delete file="..."`（**实现时用 help 确认**） |
| 多 vault | 命令首参 `vault="<name>"` |

### 10.2 直接文件访问例外清单（少数情况）

1. CLI 不可用 / Obsidian 未运行，且用户明确同意继续；
2. 需要对原始文件内容做格式校验（JSON / YAML / Canvas）；
3. 二进制附件导入（CLI 不支持二进制写入时）；
4. HTML 导出读取源文件（CLI 无法提供等价读取时）；
5. 超长内容（>4000 字符）CLI 参数可能被截断：CLI 创建占位文件后直写完整内容（v1.3.0 起）。

每次例外使用后，必须在回复中说明原因。

---

## 11. 已知事实与风险

| # | 事项 | 说明 |
|---|---|---|
| 1 | Obsidian 未运行时 CLI 行为 | 用户提供信息：首次调用 CLI 会自动打开 Obsidian；本地实测：Obsidian 未运行时 `obsidian help` 报 "The CLI is unable to find Obsidian. Please make sure Obsidian is running and try again."。实现时环境自检需覆盖两种情况：能拉起则拉起，不能则明确提示先打开 Obsidian |
| 2 | CLI 删除与回收站 | 论坛信息（2026-04）：CLI `delete` 默认直接进系统回收站，忽略 Obsidian 的 .trash 设置。实现阶段以 `obsidian help` + 安全测试为准 |
| 3 | 日记按月切分 | 官方 Daily Notes 支持日期格式含路径分隔符（`YYYY/MMMM/YYYY-MMM-DD` 示例），`YYYY-MM/YYYY-MM-DD`（年月/日）方案可行；CLI 能否直接修改该设置待实现时验证，不可行则提供一次性人工配置指引 |
| 4 | HTML 转换保真度 | 不追求与 Obsidian 渲染完全一致；目标为跨设备可读。具体转换器（CLI 导出能力 / 轻量转换脚本）在实现阶段选定 |
| 5 | CLI 版本演进 | Obsidian CLI 命令随版本变化，一切以 `obsidian help` 输出为准，references 中注明检查方法 |
| 6 | 系统 Python | 本机系统 `python` 不可用，脚本运行使用环境中可用的 Python 运行时（如工作区依赖加载的捆绑运行时）；skill 内需写明如何发现解释器 |

---

## 12. 发布约定（自 v1.0.0 起执行）

1. 更新 `CHANGELOG.md` 与 `DESIGN.md`（`vX.Y.Z - 变更描述`，含兼容性说明）；
2. 兼容性检查：旧配置（schema version）可读取或可迁移、vault 内容不受影响、已生成的用户手册不被覆盖；
3. 运行测试：`skill-creator` 的 `quick_validate.py` + 脚本自测 + 真实场景 forward-test
   （测试库：`D:\Peojects\MyProject\Skills\知识库skill测试\Obsidian测试知识库`，
   已注册；每次发布前在测试库跑核心工作流自测，v2.3.0 起）；
4. 打包：生成 zip 到 `dist/`，命名含版本号与时间戳（如 `obsidian-kb-v1.0.0-<timestamp>.zip`）；分发包仅含运行时文件（SKILL.md、agents/、scripts/、references/、assets/），开发期文档（CHANGELOG / DESIGN / REQUIREMENTS / TEST-REPORT）不随包分发；
5. Git 提交：`feat:/fix:/docs: vX.Y.Z - 描述`；
6. 用 `scripts/update_skill.py`（重写后的精简版）辅助执行；
7. **每次改动完成后自动执行发布（v2.0.1 起）**：完成任何变更后立即执行
   `update_skill.py release`（check → package → commit），不等待用户明确要求；
   版本号按语义化版本推进（patch：文档/小修正；minor：新功能；major：破坏性变更）。

---

## 13. 红线（不可违背）

1. 不开发自研引擎 / 索引 / 同步机制；
2. 写入、修改、删除只走 Obsidian CLI；删除绝不绕过 CLI；
3. 修改、删除已有内容前必须征得用户同意；
4. 不执行 `git init`（只提交，不建仓）；
5. 不把 HTML 导出写进 vault；
6. 不写死路径、vault、目录习惯；一切经配置；
7. 不强制去重、不强制标题规范；
8. 首次初始化产生的配置与用户手册只写入大知识库文件夹（vault 上级目录），绝不写入 vault 内部；
9. 每次发布必须同步更新 CHANGELOG.md 与 DESIGN.md，并保证向后兼容（破坏性变更需升主版本并提供迁移方案）；
10. 不假设 vault 名称或任何固定目录名：vault 在 agent 项目建立前已存在、名称不统一，一切以用户确认和配置为准。

---

## 14. 验收标准

- [ ] 用用户自建的大知识库文件夹（内含 Obsidian vault）完成从零初始化：配置文件与用户手册生成在 vault 上级目录，vault 内无 skill 专属残留；
- [ ] vault 使用任意用户自定义名称（非固定名，如 `MyVault`、`学习库`）均可正常接入初始化；
- [ ] 用户手册内容与 skill 内置模板一致；重复初始化不覆盖已有手册；
- [ ] 两个不同项目目录分别初始化配置，各自管理不同 vault，互不冲突；
- [ ] HTML 导出目录默认生成在大知识库文件夹内（vault 外），名称与位置经首次初始化确认；
- [ ] 开发目录中 CHANGELOG.md 与 DESIGN.md 与当前版本一致（开发期文档，不随包分发）；
- [ ] 模拟一次版本升级：旧配置可读取或按迁移说明升级成功，vault 内容与用户手册不受破坏；
- [ ] FPGA 示例（§4.1）完整跑通：记录 → 问题写入 `问题/未解决/`（文件名带日期前缀）→ 代码块无丢失 → 属性写入 → 相似检查 → Git 提交 → 反馈链接；
- [ ] 问题解决：移入 `问题/已解决/`、`status: done`、frontmatter 含 `created`（记录日期）与 `resolved`（解决日期）、TODO 勾选；沉淀：agent 提出知识类型交用户审核 → 创建 `知识/<类型>/YYYY-MM-DD 标题.md`，双向链接回原问题，原问题保留在已解决文件夹；
- [ ] 问题文件名统一 `YYYY-MM-DD 文件名.md`（未解决/已解决一致，用简短描述非长句）；每次修改文件后 `updated` 属性同步更新（精确到分钟）；
- [ ] 大知识库文件夹（vault 外）`log/YYYY-MM/YYYY-MM-DD.md` 按年月/日切分，每次操作（含读取/搜索）追加记录；
- [ ] 勾选 TODO 完成后，条目自动移入底部「已完成」折叠块（默认折叠）：最新完成在最上、完成越久越靠下，进行中条目保持原序在上（v2.1.0）；
- [ ] 知识模块按类型子目录切分（初始 7 类：经验/原理/工具/设计/规范/案例/模板），知识笔记文件名带日期前缀；
- [ ] 积极打标签：问题 / 知识 / 剪藏 / 项目笔记均带层级标签（领域 / 类型 / 来源），沉淀与相关笔记间建立双向链接；
- [ ] Bases 视图与 Canvas 画布：按用户指令创建，语法合法、可正常渲染；
- [ ] 日程：口述日程（含时间）→ 创建 `日程/YYYY-MM-DD 标题.md`，属性 `type: event` / `date` / `status` 齐全，打 `日程` 标签；完成/取消更新 status 与 updated；
- [ ] 自动化提醒：含明确时间/周期/提醒词的待办与日程自动创建提醒（平台无关能力），TODO 条目旁有「已设提醒」标注且操作日志有记录；无时间信号的待办不创建；取消提醒可查询并删除；
- [ ] 看板（可选组件）：初始化询问是否创建；创建后 `看板.md` 嵌入各 Bases 视图，问题/任务/日程/知识/项目/剪藏/日记板块聚合正常，Bases 视图可切换、排序、过滤；
- [ ] 文件类型（剪藏 / 模板 / 附件 / Bases / Canvas）：按用户指令存放于用户指定位置，不预设固定目录；
- [ ] 无法判定分类时询问用户归属，不写入收件箱（v2.0 起无收件箱）；
- [ ] 原生日记按月分目录生成（`日志/2026-08/2026-08-02.md` 形态）；
- [ ] 网页剪藏生成带 `source_url` / `source_domain` 的结构化笔记；
- [ ] HTML 镜像导出目录结构与 vault 对应，`index.html` 可打开、手机浏览器可读；新增/修改/删除笔记后镜像自动同步更新；
- [ ] 删除一条测试笔记后，可在系统回收站找到并恢复；
- [ ] 修改/删除已有内容前均有用户确认环节；
- [ ] 多 vault 切换与指定操作正常；
- [ ] 写入高相似内容时提示用户且由用户决策，不被强制阻断；
- [ ] Windows 全流程可用；代码无 Windows 专属依赖（为 macOS 预留）；
- [ ] SKILL.md 通过 `quick_validate.py`，正文 < 500 行。
