# 知识库专属规则（本库定制）

> 本文件记录**当前知识库**的专属规则与红线，是默认流程（以
> `knowledge-workflow` 流程规范为准）的补充与覆盖。
> **每次操作前必须先读取本文件**；内容以实际记录为准，模板小节可增删改。

## 本库独有模块

（记录本库在默认模块之外新增的模块目录、命名规则与管理方式。示例：
- `实验记录/` —— 每次实验一篇，`type: experiment`，只追加不修改
- `复习卡片/` —— 按主题分文件夹，`type: card`）

## 本库专属规则与偏好

（记录本库特有的处理规则。示例：
- 剪藏统一存 `剪藏/待整理/`，整理完移入对应模块
- 问题文件命名用 `<主题>-<日期>` 而非默认的 `<日期> <主题>`）

## 本库额外红线

（记录本库不可违背的额外红线。示例：
- `实验记录/` 内容永不删除、永不覆盖
- 涉及 `客户/` 目录的笔记禁止导出为 HTML）

## 默认流程之外的操作要求

（记录 agent 在本库必须额外执行的动作。示例：
- 每次记录后向用户汇报本库规则命中情况
- 每月 1 日提醒用户做月度归档）
