# Agent 规则（本库定制）

> **每次操作前必须先读取本文件**，再按其中规则执行；本文件是默认流程
> （knowops 规范）的补充与覆盖。

## 插件集成规则（初始化时与用户确认后填写）

（示例格式，按实际确认结果改写）
- 插件 A（版本管理）：修改知识库后自动提交，提交信息含简述；
- 插件 B（云同步）：提交后自动同步；
- 执行顺序：先 A 再 B；
- 插件变动（安装/卸载/改规则）时更新本节并记入「变更记录」。

## 本库额外红线

（记录本库不可违背的额外红线。示例：
- 某目录内容永不删除、永不覆盖；
- 涉及某目录的笔记禁止导出为 HTML。）

## 默认流程之外的操作要求

（记录 agent 在本库必须额外执行的动作。示例：
- 每次记录后向用户汇报本库规则命中情况；
- 每月 1 日提醒用户做月度归档。）

## 旧库/特殊结构约定

（本库与默认目录结构不一致的处理结论，例如：保留旧目录、部分调整、全量重构；
结论由用户确认后写入，agent 不自动迁移。）