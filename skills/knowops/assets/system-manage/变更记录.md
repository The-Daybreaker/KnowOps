# 变更记录（本库定制）

> 记录知识库结构与规则的重要变更：日期、变更内容、原因、依据（用户确认/审阅）。
> 每次结构或规则变更后追加，不覆盖历史。
> 首次初始化时，将下方日期与版本号改为当天实际值。

## 2026-08-10

- 初始化：按当前版本模板创建本系统文档（版本见初始化说明）；
- 模块结构：00 收件箱 / 01 知识 / 02 摘录 / 03 系统 / 04 归档；根目录
  `看板.md` 一屏总览（看板不是模块）；
- 约定：系统、归档固定位于末两位；日记在 `03 系统/日记/`（按年切分，自动
  记录操作）；模板在 `03 系统/模板/`；插件与同步规则在初始化时与用户确认后
  由 agent 自动执行。
