# 变更记录（本库定制）

> 本文件位于 `<vault>/.config/变更记录.md`（隐藏目录，Obsidian 文件树不显示，
> 用文件管理器打开库目录可见），由 agent 在库的结构或规则变更后追加维护：
> 日期、变更内容、原因、依据（用户确认/审阅）。每次变更后追加，不覆盖历史；
> 单篇归档等常规操作只记日记，不进本文件。
> 首次初始化时，将下方日期改为当天实际值。

## 2026-08-10

- 初始化：按当前版本模板创建本系统文档（版本见初始化说明）；
- 模块结构：基础四模块（00 收件箱 / 01 知识 / 03 系统 / 04 归档）＋已登记
  扩展模块（清单与规则见用户手册「模块规则」章节，config
  `preferences.modules` 为机器索引）；根目录 `看板.md` 一屏总览（看板不是
  模块）；
- 约定：系统、归档固定位于末两位；日记在 `03 系统/日记/`（遵循 Obsidian
  Daily notes 规则按年切分，分「用户/agent」两章记录操作）；模板在
  `03 系统/模板/`；插件与同步规则在初始化时与用户确认后由 agent 自动执行。
