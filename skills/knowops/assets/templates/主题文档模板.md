---
type: knowledge
created: "{{date}}"
updated: "{{date}}"
tags: []
---

# {{title}}

<!-- 用法（正式使用时请删除本注释块）：
     一个主题一份文档，条目按章节组织，新条目追加到对应章节末尾。
     章节名要自解释（如「架构设计规范」）；不自解释的，在章节标题下一行
     写使用场景（如：这是给用户设计提示词的时候用的）。
     条目标题写成「一句可直接执行的结论」；正文完整保留用户原话（不改写、
     不压缩），可选「补充」小节写背景与动机（经用户同意后写）。
     单章条目超过约 30 条时请 agent 拆分。 -->

## 示例章节

### 示例条目：标题是一句可直接执行的结论

> 原话：（用户原话完整记录，原样保留——怎么说的就怎么记，不提炼、不改写、
> 不压缩。）

补充（可选，经用户同意后写）：背景、动机与适用场景。信息写全，不为省字
而精简；说人话，不堆专业词。
