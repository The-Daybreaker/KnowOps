# agent-rules（本库个性化约束，agent 读）

> 本文件存于 `<vault>/.config/agent-rules.md`，是 agent 每次变更操作前必读的
> 个性化约束；人可编辑。默认流程见 knowops 规范；本文件只记录「与默认不同」的
> 补充/覆盖，无差异的项可留空。

## 插件集成规则（初始化时与用户确认后填写）

（示例格式，按实际确认结果改写）
- 插件 A（版本管理）：修改知识库后自动提交，提交信息含简述；
- 插件 B（云同步）：提交后自动同步；
- 执行顺序：先 A 再 B。

## 本库额外红线

（记录本库不可违背的额外红线。示例：某目录内容永不删除、永不覆盖。）

## 默认流程之外的操作要求

（记录 agent 在本库必须额外执行的动作。示例：每次记录后汇报规则命中情况。）

## 旧库/特殊结构约定

（本库与默认目录结构不一致的处理结论，由用户确认后写入，agent 不自动迁移。）
